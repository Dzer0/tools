#拉取镜像
pull1.sh image_name:tag 
#上传镜像
push1.sh image_name:tag
#注意当前版本不支持跟多个镜像名，一次只能上传一个。可以自己写个for脚本批量上传。
#第一次拉取前，需要在确定虚拟机可以联网（可以用ping www.badiu.com 命令确认）情况下，执行date.sh脚本同步虚拟机时间到当前时间，否则拉取上传镜像会报错。
#如果无法联网，可以用 下列命令设置虚拟机时间为当前时间（注意替换日期，不要照搬）
date -s "2016-06-02 13:58:00"
